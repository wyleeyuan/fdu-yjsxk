# 复旦研究生选课脚本（2026 版）

选课开放时间：**2026-09-02 10:00:00 ~ 2026-09-21 10:00:00**

## 怎么用

| 想做的事 | 操作 |
|---|---|
| 开抢前先验证一下 | 双击 **`先跑自检.command`** |
| 正式抢课 | 双击 **`一键抢课.command`** |

流程就两步：**先自检，看到"自检通过"再一键抢课。**

双击 `一键抢课.command` 后脚本会自动：从 Edge 读 Cookie → 等到 09:59:55 → 开抢
→ 10:30 收工。**全程不用手动复制 Cookie**，也**不需要连校园网 VPN**（校外直连可用）。

自检会告诉你四件事：能不能读到 Cookie（15 个字段）、Cookie 是否有效、csrfToken
能不能取到、当前启用哪几门课。四项都正常才会打印「自检通过」。

如果自检报错，通常是**浏览器登录态掉了**，去 Edge 打开一次
`http://yjsxk.fudan.edu.cn` 重新登录就好。

## 这次抢哪些课

来自 `复旦研究生选课_更新版.xlsx`，班级代码已从选课系统核实过。当前启用 **7 门**：

| # | 课程 | 班级代码 | 上课时间 | 容量 |
|---|---|---|---|---|
| 1 | GPU 程序设计 | `2026202701CS50014.01` | 周五 3~5 节 JB102 | 60 |
| 2 | 深度学习应用 | `2026202701EIE60022.01` | 周四 3~5 节 JA102 | 80 |
| 3 | 程序分析 ⚠ | `2026202701CS60027.01` | 周三 11~13 节 JB103 | 80 |
| 4 | 计算思维与算法实践 | `2026202701CS50002.01` | 周一 1~3 节 JA303 | 100 |
| 5 | 互联网应用技术 | `2026202701CS70026.01` | 周一 6~8 节 JB100 | 150 |
| 6 | 高级软件开发技术 | `2026202701EIE50009.01` | 周五 11~12 节 JB101 | 150 |
| 7 | 马克思主义与社会科学方法论 | `2026202701GEIP40008.03` | 1~8 周 周四 6~7 节 JA205 | 180 |
| ⛔ | ~~软件过程管理与工程伦理~~ | `2026202701EIE60029.02` | 周四 11~13 节 JA205 | 120 |

**选课是一门一门来的**（系统提交后要等这门出结果，才能选下一门），所以顺序就是优先级：
容量最小的排最前，最难抢的先下手。GPU（60 人）第一个，马哲（180 人）排最后不慌。

⛔ 「软件过程管理与工程伦理」已停用（`enabled: false`），不参与抢课。
以后想抢就把 `config.json` 里那项的 `enabled` 改回 `true`，代码还留着，不用重抓。

⚠ 「程序分析」在 Excel 里标的是"暂定，可能退课"。不想抢就打开 `config.json`，
把那一项的 `"enabled"` 改成 `false`。其余课程同理。想调整先后顺序，直接改 `courses`
数组里的排列即可。

## 想改什么，改 config.json 就行

```jsonc
"start_time": "2026-09-02 09:59:55",   // 开抢时间（提前 5 秒起跑）
"end_time":   "2026-09-02 10:30:00",   // 到点强制收工
"browser":    "edge",                   // 从哪个浏览器读 Cookie
"request_interval": 0.8,                // 每门课提交之间的间隔（秒）
"serial_mode": true,                    // 一门一门来，务必保持 true
"poll_max":    15,                      // 一门课最多轮询几次，防止卡住后面的课
```

课程条目里的 `lx` / `bqmc` 是选课系统的分类编码，已按 2026 年真实请求填好，别手改。

### 为什么是一门一门选

选课系统的结果轮询在网页里是**单例**的（全局一个"正在查询"标志）。如果一下把几门课
都提交出去，后一次会覆盖掉前一次的轮询，导致前面那门的结果收不到、白白丢失。

所以 `serial_mode` 必须保持 `true`：脚本会「提交一门 → 等这门出结果 → 再提交下一门」。
顺序即优先级，已按容量从小到大排好。

## 和原版 course.py 的差别（为什么要重写）

原仓库 `JarynWong/fdu_course_enrollment` 基于 2024 年协议，直接跑会有三个问题：

1. **csrfToken 取不到** —— 原正则是 `value='...'` 单引号，现在页面是 `value="..."`
   双引号而且 `id` 前面多了 `style` 属性。结果就是 Cookie 明明有效，却永远报
   "cookies过期"。
2. **把"已受理"当成"已选上"** —— 现在选课是两步：先 `choiceCourse.do` 拿受理号
   `xid`，再轮询 `loadXkjgRes.do`，返回 `code==1` 才算真的选上。原脚本拿到
   `code!=0` 就打印成功并退出，会漏课。
3. **Cookie 要手抄** —— 现在用 `browser-cookie3` 直接读浏览器本地库，能拿到
   HttpOnly 的 `JSESSIONID`（页面 JS 读不到这个），真正一键。

原版文件留在 `原版备份/` 里。

## 几个注意点

- **Cookie 有效期约 3 小时**，脚本会在开抢前 3 分钟自动换新票，长时间作战时每 4 分钟换一次，不用管。
- 脚本每轮之间有 0.8 秒间隔，不是死循环狂刷，别把它调太小。
- 抢课过程中你在浏览器里操作选课网站会频繁提示"页面过期"，这是正常的，脚本结束后就恢复。
- 结束后务必去「已选课程」页核对一遍。
